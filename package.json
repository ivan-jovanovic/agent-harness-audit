{
  "name": "agent-harness-cli",
  "version": "0.1.0",
  "description": "Audit a project for agent-readiness before giving AI coding agents broad autonomy.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "bin": {
    "agent-harness": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "@types/node": "^25.5.0",
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
